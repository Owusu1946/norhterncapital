import { Inngest } from "inngest";

/**
 * Inngest client for Northern Capital Hotel
 * Used for background jobs like report generation
 */
export const inngest = new Inngest({
    id: "northern-capital-hotel",
    name: "Northern Capital Hotel",
});
